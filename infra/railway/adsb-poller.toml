# Railway service: adsb-poller-py
# GATEWAY_URL=http://backend.railway.internal:${{backend.PORT}}

[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/docker/Dockerfile.adsb-poller"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
